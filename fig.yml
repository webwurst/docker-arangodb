arangodb:
  build: .
  ports:
   - "8529:8529"
  volumes:
   - ${HOME}/.docker_volumes/${VOLUMES}/databases:/var/lib/arangodb/databases
   - ${HOME}/.docker_volumes/${VOLUMES}/apps:/var/lib/arangodb/apps
