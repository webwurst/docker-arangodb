arangodb:
  build: .
  environment:
    DEVELOPMENT: true
  ports:
    - "8529:8529"
  volumes:
    - docker_volumes/databases:/var/lib/arangodb/databases
    - docker_volumes/apps:/var/lib/arangodb/apps
